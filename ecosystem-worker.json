{
  /**
   * Application configuration section
   * http://pm2.keymetrics.io/docs/usage/application-declaration/
   */
  apps : [{
    // Worker
    "name"        : "guerilla-worker",
    "script"      : "server.js",
    "args"        : ["--worker", "--config", "/Users/guser/Workspace/worker/guerilla/deploy/config/" + (process.env.GuerillaHostName || "process.env.GuerillaHostName not found") + "/config/worker/config.json"],
    "watch"       : false,
    "node_args"   : "--harmony",
    "cwd"         : "/Users/guser/Workspace/guerilla"
  }],

  /**
   * Deployment section
   * http://pm2.keymetrics.io/docs/usage/deployment/
   */
  deploy : {
    production : {
      user : "node",
      host : "a.b.c.d",
      ref  : "origin/deploy-nyc1",
      repo : "git@github.com:repo.git",
      path : "/Users/guser/Workspace/worker",
      "post-deploy" : "npm install ; pm2 startOrRestart ecosystem-worker.json --env production"
    },
    dev : {
      user : "guser",
      host : "172.16.221.128",
      ref  : "origin/deploy-nyc1",
      repo : "git@git.corp.yahoo.com:bruceg/yahoo-guerilla.git",
      path : "/var/www/development/worker",
      "post-deploy" : "npm install ; pm2 startOrRestart ecosystem-worker.json --env dev",
      env  : {
        NODE_ENV: "dev"
      }
    }
  }
}
